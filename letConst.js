// const full meaning (constant)
// const it's not changeable and it use just for fixed element which is not use more then one.

// (let) we can use it many times when we need to declare a variable.
console.log(50);